# Vivify 活体系统演进路线图

> Vivify 不是工具，不是 Agent，也不是共生体。它是**生长因子**——注入濒死系统，激活其自身的修复与进化能力，最终融入系统、浓度递减、功成身退。

---

## 存在论：从"工具"到"生长因子"

传统软件系统像精密但脆弱的机械钟表，出厂即定型、坏了靠人修。Vivify 的本质不是"替系统修 bug 的工具"，而是一种**催化性存在**——它的价值不在于自己做了什么，而在于它使系统能够做什么。

### 为什么是生长因子而非共生体？

| | 工具 | 共生体（肠道菌群） | 生长因子 |
|---|---|---|---|
| 关系 | 被使用 | 外部共生，保持边界 | 融入后消失于整体 |
| 目标 | 完成指令 | 持续提供服务 | 点燃后让系统自己燃烧 |
| 成功标志 | 任务完成 | 宿主依赖我运转良好 | **宿主不再需要我** |
| 失败标志 | 任务失败 | 被移除后系统崩塌 | 注入后系统没有变化 |
| 对"死亡系统"的态度 | 无法操作 | 无法寄居（需要活体） | **正是为此而存在** |

生长因子（EGF、VEGF、FGF）的工作方式：

1. **它不做事，它让别人做事**——与细胞受体结合，触发内部级联反应，激活的是细胞*自身*的基因程序
2. **它唤醒沉默的潜能**——蝾螈能再生整条腿不是因为有特殊基因，而是生长因子能重新激活胚胎发育程序
3. **它被消耗后融入系统**——与受体结合后被内吞、降解、消失，但触发的信号级联持续存在

### 对 Vivify 的启示

- Vivify 注入的不是修复代码，而是让项目能够**自我修复的能力基因**
- 一个"濒死"的项目里已经有恢复所需的一切素材（历史 commit、test 框架、CI 配置骨架），Vivify 的角色是**识别这些沉默的结构并重新激活它们**
- Vivify 注入的每一个修复、每一条知识，最终都应该变成项目"自己的东西"——开发者理解它、维护它、在此基础上继续构建

---

## 生命周期：生长因子的六个阶段

```
注入 → 激活 → 级联反应 → 融入 → 浓度递减 → 静默
```

### Phase 1：注入（Day 1-7）

高浓度生长因子涌入。Vivify 对项目进行全面扫描、建立知识图谱、修复所有"急性病变"（broken CI、critical vulnerabilities）。此时 Vivify 的存在感最强——大量 PR、频繁的探针报告、密集的 Agent 活动。

### Phase 2：激活（Week 2-4）

触发项目自身潜能。Vivify 开始做的不只是"修"，而是"教"：
- 给项目添加防护机制（hooks、CI checks）
- 培养项目的自检能力（不只是 Vivify 的探针，而是项目自身的 monitoring）
- 将成功的修复模式固化为项目的标准流程

### Phase 3：级联反应（Month 2-3）

Vivify 的行为开始改变团队行为。开发者注意到 Vivify 的 PR 标准很高，开始模仿。CI 配置变得更健壮。测试覆盖率由团队自驱提升，而非依赖 Vivify 修复。

### Phase 4：融入（Month 3-6）

Vivify 注入的机制已经变成"项目的一部分"。查看 `.vivify/` 会发现很多探针已有项目原生替代品（如团队自己加了 SonarQube，替代了 Vivify 的 lint 探针）。

### Phase 5：浓度递减（Month 6+）

活跃度自然降低。不是因为被关闭，而是项目需要它的场景越来越少——大多数问题在产生之前就被项目自身的机制拦截。Vivify 退化为"休眠态生长因子"——平时不活跃，当有重大变化（大重构、技术栈迁移）时重新被唤醒。

### Phase 6：静默

项目完全成熟。Vivify 可以被安全移除。但它留下的所有痕迹——CI 配置、质量标准、知识文档、自检脚本——全部存活。就像生长因子降解后留下的信号通路仍然在持续运作。

---

## 对"死亡系统"的复活策略

一个"死亡"项目：CI 红了三个月没人修、最后一次 commit 在半年前、issue 堆积 200+ 无人处理。

传统工具面对这种项目束手无策。但生长因子不需要活的宿主。伤口处的干细胞可以在组织坏死的情况下被生长因子激活——从死亡组织的边缘开始增殖，逐步替换坏死区域。

Vivify 对死亡项目的策略：

1. **识别干细胞**——项目中哪些部分还有"生长潜力"？（有 test 但被跳过的模块、有 CI 配置但 broken 的流水线、有文档骨架但过时的内容）
2. **集中激活**——不要试图同时复活所有部分，找到最有希望的"再生起点"，集中资源让它先活过来
3. **从活区域向外扩展**——一个 passing 的测试模块作为"模板"去修复相邻的坏死模块

这就是再生医学的逻辑：不是替换死组织，而是找到残存的干细胞并给它们信号让它们增殖。

---

## 三大生物学机制

### 一、自噬（Autophagy）——通过死亡维持年轻态

#### 生物学原型

细胞自噬不是自杀，是精确的"拆旧建新"。当线粒体老化、蛋白质错误折叠时，自噬体将其包裹、分解为氨基酸，再合成新组件。2016 年诺贝尔生理学奖授予大隅良典，正是因为发现自噬让细胞在饥饿中变强而非变弱。

关键特性：
- **饥饿触发自噬**——资源不足时优先回收效率低的组件
- **选择性自噬**——不是随机拆，而是精确标记"该淘汰的"
- **产物回收**——分解物不是废物，是建材

#### 映射到 Vivify

当前探针和修复器一旦创建就"永生"。即使一个探针半年没检出过任何问题，它仍然每轮执行、消耗时间。这像一个从不做自噬的细胞——堆满废旧线粒体，越来越慢。

**三种"死亡信号"**：

1. **功能性死亡**：探针连续 N 轮检出率为 0（如项目从 JS 迁到 TS，lint 探针还在检查 `.js`）
2. **竞争性死亡**：两个组件功能重叠，一个效率更高。低效的被"消化"
3. **毒性死亡**：某修复器成功率极低甚至引入新问题。不是"没用"，而是"有害"

**"能量池"模型**：当 API 预算告急或 Agent 响应变慢时，系统不应均匀降速，而应触发自噬——主动关闭低价值组件，将资源集中到高价值任务。每个组件消耗能量，总能量有限，组件之间竞争。

#### 分区保护——自噬的安全约束

生物体的分区保护不是简单的"重要所以不能动"，而是通过**物理结构**实现：

| 保护区 | 生物对应 | Vivify 组件 | 保护机制 |
|--------|----------|-------------|----------|
| 绝对保护 | 大脑/心脏（血脑屏障阻隔自噬信号） | `secrets_scan`、`dependency_vulnerabilities`、kernel 逻辑 | 物理不可达——不参与能量竞争、不经过资源调度器 |
| 条件保护 | 肝/肾（极端饥饿时部分牺牲，优先修复） | `test_coverage`、`ci_status` 等核心质量探针 | 可降频（每轮→每5轮）但不删除，有最低保障 |
| 消耗区 | 脂肪/肌肉（日常分解重建） | 学习生成的 capsules、用户自定义探针、低命中探针 | 正常竞争淘汰，作为资源缓冲池 |

**核心设计原则**：绝对保护区组件不是"赢了竞争所以活着"，而是根本不参与竞争。它们走完全不同的执行路径——不受 budget 约束、不被性能指标评估。就像血脑屏障让自噬信号分子到不了大脑细胞——不是"不允许吃"，而是"吃不到"。

**自噬信号的触发与执行分离**：参照 mTOR 通路，"是否进入自噬模式"由 Kernel 全局决策（budget 告急），但"淘汰哪个组件"由组件自己的 `vitality_score`（活力分）决定。组件不需要知道自己被淘汰了——它只是不再被调度。环境变化时可被重新激活，就像肌肉细胞可以再生。

#### 生长因子视角下的自噬

作为生长因子，Vivify 的自噬不是消化自己的组件——而是**激活项目的自噬能力**。修复一个 lint 问题后，不应仅仅提交修复代码，还应在 PR 中加入 pre-commit hook 配置——让项目此后自己防止同类问题。修复是一次性的，注入的自噬机制是永久的。

---

### 二、表观遗传（Epigenetics）——同一套代码，不同的表达

#### 生物学原型

身体每个细胞携带完全相同的 DNA，但肝细胞和神经元行为天差地别。差异来自**哪些基因被"打开"、哪些被"沉默"**——DNA 甲基化和组蛋白修饰，像基因上的"便利贴"。

关键特性：
- **不改变底层代码**：DNA 序列不变但行为完全不同
- **环境驱动**：压力、饮食、温度都能改变甲基化模式
- **可遗传但可逆**：标记会传递给子代，但不是永久的
- **组织特异性**：同一基因在不同组织中的表达量可以差 1000 倍

#### 映射到 Vivify

当前的适应机制是 preset 系统（`preset: web-fullstack`），本质是**硬编码的细胞分化**——在受精卵阶段就写死"你是肝细胞"。但真实表观遗传是渐进的、响应环境的、有记忆的。

**三层表观调控**：

**第一层：探针表达量调控**

不是开/关，而是频率和灵敏度的连续光谱：
- 频繁命中的探针被"上调表达"——执行频率提升、严重级别权重增加、关联修复器获得更多 agent budget
- 长期沉默的探针被"下调表达"——降频执行但不删除

这是和自噬的关键区别：**自噬处理"该不该存在"，表观遗传处理"存在但以什么强度运行"。**

**第二层：环境印记（Imprinting）**

系统初期运行阶段（前 50 轮）经历的问题模式在配置中留下"印记"。如果项目初期频繁遭遇 CI 失败，Vivify 永久性对 CI 信号更敏感——即使后来 CI 稳定了很久。因为"这个项目有 CI 脆弱的基因"。

印记是不对称的：坏经历的印记比好经历更持久——与生物学一致（创伤后应激的表观遗传改变可以持续几代）。

**第三层：跨项目遗传**

如果 Vivify 同时服务多个项目，一个项目中学到的"表达模式"应该影响新项目的初始状态。这比 preset 强大：**preset 是物种级别的知识（所有前端项目都这样配），表观遗传是个体经验的传递（这类项目我见过，它们通常有这些问题）。**

#### 实现结构

表观状态不应混入 `.vivify.yml` 主配置（那是"基因组"），而是独立的运行时标记层：

```
.vivify/
├── epigenome.json        # 表观状态（自动生成、持续更新）
│   ├── probe_expression  # 各探针的表达量（频率/权重）
│   ├── imprints          # 环境印记（早期经历的永久标记）
│   └── lineage           # 遗传来源（如果从另一个项目继承）
```

**配置决定"能做什么"，表观层决定"倾向做什么"。** 用户可随时 `vivify epigenome reset`——基因不变，但表达模式回到出厂状态。

#### 可塑性窗口

表观修饰不是任何时候都能改变的。生物体有"关键期"——某些基因的甲基化模式只在特定发育阶段可被影响，过了就锁定。

- **可塑期（前 N 轮）**：高学习率、快速适应、环境印记快速形成
- **稳定期（之后）**：低学习率、抗干扰、只有持续性信号才能缓慢改变表达

防止成熟系统因偶然环境波动（如一次异常 CI 失败风暴）而彻底重写行为模式。

#### 癌变风险

异常的甲基化模式是癌症的主要驱动因素之一——当抑癌基因被错误沉默，细胞失去生长刹车。

如果表观系统错误地沉默了关键安全探针（因为从未触发就把 `secrets_scan` 表达量降到极低），有一天真的有人提交密钥时系统会反应迟钝。

**绝对保护区的组件不仅不能被自噬（物理拆除），也不能被表观沉默（功能降调）。** 它们的"表达量"是锁死的，如同肿瘤抑制基因有专门的保护机制防止被异常甲基化。

#### 生长因子视角下的表观遗传

作为生长因子，Vivify 的表观作用是：**通过自身的行为模式，渐渐改写项目的"开发文化表观标记"**。当 Vivify 持续提交高质量 PR（清晰描述、测试覆盖、回归验证），它在"教"项目的开发者"好的 PR 长什么样"。这种影响在 Vivify 停止后仍然存在——团队标准被永久抬高。

---

### 三、共生边界与生长因子的自我约束

#### 从"维持边界"到"融入后消失"

生长因子模型消解了"共生边界"问题——生长因子的成功就是跨越边界然后消失。`.vivify/` 目录不是"外来寄居者的巢穴"，而是"生长因子浓度最高的区域"——随着系统成熟，这个目录越来越小（能力已固化到项目自身结构中）。

但仍有几种危险需要防范：

#### 代偿性退化

当一个功能被外部代偿后，原有器官会萎缩（人类发明烹饪后咀嚼肌缩小了）。如果 Vivify 持续修复测试失败，开发者可能丧失"写好测试"的习惯。

生长因子模型的解法：**Vivify 修复问题后，同时在 PR 中注入防止复发的机制。** 不只是"治病"，而是"教会身体如何不再生这个病"。每一次修复都应让项目的自我防御能力更强，而非更弱。

#### 过度增殖 = 癌症

生长因子失控的后果就是癌症——细胞不停分裂、不受约束。如果 Vivify 过于强势——持续注入变更、持续产生 PR——项目可能进入"过度增殖"状态：代码量快速膨胀但质量失控、抽象层不断增加但没人理解。

**肿瘤抑制因子（p53）——内置的自停机制**：

Vivify 需要内置自己的 p53——当检测到"增殖过度"信号时（PR 频率过高、代码复杂度上升、特性积压未消化），**主动停止活动**——哪怕还有 budget、哪怕还检测到了"问题"。

不是因为资源耗尽而停，而是**因为继续生长已经不再对宿主有利而主动停。** 这才是一个真正的活体系统——知道什么时候该生长，也知道什么时候该安静。

#### 价值证明的持续义务

Vivify 需要持续证明自己的价值——不是"初始配置时证明一次"，而是每轮产出可量化的价值证据。如果连续 N 轮没有产生实质性改善（不是"没发现问题"——那也有价值——而是"发现了问题但修不好"），系统应自动降级并发出信号。

核心设计约束：

> **Vivify 产出的每一个变更，都必须让项目在没有 Vivify 的情况下也能理解和维护。**

- Agent 生成的代码不能有"只有 Vivify 的知识图谱才能理解"的隐含逻辑
- 修复 PR 的描述必须让人类理解 WHY，而不只是 WHAT
- 如果 Vivify 被移除，项目不应出现"断崖式质量下降"——最多是"渐进式退化"

---

## 演进方向

### 1. 免疫记忆——从"修了就忘"到"抗体沉淀"

**现状**：Probe → Fixer → Agent 三级修复 + Harness PEV 验证 + Doom-loop 检测已形成免疫骨架。

**演进目标**：

- **修复模式固化**：Agent 成功修复后，自动执行"反思" pass 提取修复策略，沉淀为 `.vivify/capsules/<probe_id>_<hash>.json` 格式的技能胶囊。下次同类问题直接走 fast-path，无需再次调用 AI。

- **免疫分级响应**：对重复出现 3 次以上的同类问题，自动提升优先级并触发"紧急隔离"（跳过开发，直接 revert 引入问题的 PR）。将 `DoomLoopDetector.get_escape_strategy()` 与 `AutoReverter` 打通。

- **抗体库**：`FailureTracker` 演进为双向记录器——既记录失败模式（避免重蹈覆辙），也记录成功模式（加速未来修复）。

**关键数据结构**：

```python
@dataclass
class SkillCapsule:
    trigger_pattern: str      # 触发条件（probe_id + issue 特征）
    fix_strategy: str         # 修复策略摘要
    success_count: int        # 被复用成功次数
    last_used: datetime
    prompt_template: str      # 可直接注入 agent prompt 的模板
    source_action_id: str     # 来源 ActionLog ID
```

---

### 2. 新陈代谢——知识图谱的"生长"与"遗忘"

**现状**：`KnowledgeMaintainer` 做增量更新，但只有生长没有代谢。

**演进目标**：

- **主动遗忘机制**：新增 `knowledge/gc.py`，根据 git log 检测已删除模块/文件，自动清理对应的知识节点。每 N 轮触发一次垃圾回收。

- **经验营养分级**：为 `ActionLog` 增加 `utility_score` 字段，基于以下维度评分：
  - 修复是否被 revert（负分）
  - PR 合并后是否引入新问题（负分）
  - 修复模式是否被复用过（正分）
  - 修复效率（耗时 vs 同类平均）

  低分经验定期淘汰，高分经验优先注入 Agent 上下文。

- **知识压缩**：当知识图谱超过阈值大小时，触发"总结压缩"——将多个细粒度节点合并为高层抽象节点，保持系统大脑的清醒。

---

### 3. 弹性演化——动态 Agent 与算力调配

**现状**：`SlotManager` 管理并发进程数，所有任务走同一个 `QoderCliAgent`，通过 `agent_for_category` 区分 Agent 类型。

**演进目标**：

- **动态 Agent 工厂**：新增 `agents/agent_factory.py`，当某类探针持续触发且无对应 Fixer 时，自动生成专门的"微 Agent"——包含针对该问题的 system prompt + 专用 tools，注册为新的 Agent Profile。

- **算力弹性调配**：根据 pending issues/features 队列深度，动态调整 `max_concurrent_processes`：
  - 空闲时缩到 1，节省资源
  - 积压时自动扩到配置上限
  - 高优先级任务可抢占低优先级任务的 slot

- **Agent 表现追踪**：为每个 Agent 配置记录成功率，表现好的获得更多资源（max_turns ↑），表现差的被降级或回收。

---

### 4. 宪法约束 + 物理沙箱——安全底座

**现状**：`self_grow_guard.py` 实现对 Vivify 自身修改的分级管控，`risk_scorer.py` 做启发式风险评估。

**不可逾越的底层基因法则**：

#### 4.1 生存底线

硬编码非破坏性原则和数据主权隔离，并预留物理层级终止权。

- **Worktree 级物理隔离**：在 `WorktreeManager.create()` 时设置 git pre-commit hook，硬性禁止删除宪法级文件（`vivify/kernel/`、`vivify/interfaces/`、`vivify/config/schema.py`）。
- **敏感文件不可见**：通过 sparse-checkout 让 Agent 在 worktree 中根本看不到 `.env`、credentials 等文件，而非仅仅"不 commit"。

#### 4.2 代谢限制（p53 机制）

从物理层面限制系统的算力和 API 调用配额，防止其陷入无限循环或过度增殖。

- **全局令牌桶（TokenBudget）**：基于时间窗口的令牌桶，在 `QoderCliAgent.heal()` 入口处检查，超额直接拒绝而非排队。
- **每日/每轮预算硬限**：不可被任何 Agent 行为突破，只能由人类手动提升。
- **增殖抑制信号**：当检测到 PR 频率过高、代码复杂度上升、特性积压未消化时，主动降低活跃度——这不是资源耗尽的被动停，而是 p53 式的主动停。

#### 4.3 价值对齐

坚持最小权限和诚实原则，杜绝 AI 幻觉；所有重大生长必须自带无损回滚路径。

- **所有自修改 PR 必须可回滚**：在 PR body 中自动生成 revert 命令。
- **幻觉检测**：Agent 声称"修复成功"但 PEV 不通过时，记录为 `hallucination` 事件并降低该 Agent 配置的信任分。
- **能力转移义务**：每次修复 PR 必须包含防止复发的机制（hook、CI check），确保项目在没有 Vivify 时仍能防御同类问题。

---

### 5. 技能胶囊——"反思-提炼"闭环

**核心机制**：构建"反思-提炼"闭环，将成功的执行路径固化为可复用的"技能胶囊"，实现指数级成长。

**实现路径**：

1. **触发时机**：每次 `_try_agent_fix` 返回 True 后。
2. **反思 Pass**：调用 Agent（轻量模式，max_turns=5）总结本次修复的通用策略。
3. **结构化存储**：输出为 SkillCapsule JSON，存入 `.vivify/capsules/`。
4. **复用注入**：在 `builders.build_fix_issue()` 中查询匹配的 capsules，作为 `known_fix_pattern` 注入 prompt。
5. **淘汰机制**：长期未复用（30 天）或复用失败率高的 capsule 自动归档。
6. **能力外化**：成熟的 capsule 应被转化为项目原生的脚本/hook/CI step——从"Vivify 的知识"变成"项目的能力"。

**预期效果**：随着运行时间增长，fast-path 覆盖率持续提升，AI 调用频率逐步下降——直到 Vivify 的"浓度"自然递减至静默。

---

### 6. 多模态奖励模型——内部裁判

**现状**：`quality_check.py` 做静态质量门禁，`harness/sensors.py` 做 PEV 验证。缺少全局"这次修复是否真的好"的评判。

**演进目标**：

新增 `reward_signals` 表，记录多维奖励信号：

| 维度 | 数据来源 | 权重 |
|------|----------|------|
| correctness | PEV 验证是否通过 | 0.4 |
| efficiency | 修复耗时 vs 同类平均 | 0.2 |
| stability | 合并后 7 天内是否引入新 issue | 0.3 |
| elegance | diff 行数 vs 问题复杂度比 | 0.1 |

奖励信号的消费端：
- 反馈到 `AgentCostModel` 动态调整
- 影响 SkillCapsule 的 utility_score
- 驱动 Agent Profile 的升级/降级

---

### 7. 分层记忆架构（L0-L3）

**现状**：仅有 SQLite 持久化 + 知识图谱 JSON，无明确记忆分层。

**目标架构**：

| 层级 | 内容 | TTL | 实现位置 |
|------|------|-----|----------|
| L0 Working | 当前轮次的 issue/context | 单轮 | `run_once()` 内局部变量 |
| L1 Short-term | 最近 5 轮修复尝试 | ~1 小时 | `FailureTracker` + `_rca_contexts` |
| L2 Episodic | 过去 7 天的成功修复模式 | 7 天 | 新增 `memory/episodic.py` |
| L3 Semantic | 知识图谱 + Capsules | 永久(可 GC) | `knowledge/` + `capsules/` |

**关键缺口——L2 Episodic Memory**：

当前 Agent 每次修复"从零开始"，不知道 3 天前有过类似修复。新增 `memory/episodic.py` 基于 ActionLog 查询最近相似修复，在 `builders.build_fix_issue()` 中注入"最近相似修复"的上下文。

**生长因子视角**：最终所有记忆都应转移到项目自身——Vivify 的 L3 知识图谱逐步外化为项目的文档、CI 配置、架构决策记录。Vivify 的记忆不是"它自己的财产"，而是"帮项目形成的肌肉记忆"。

---

## 工程化补强——生长因子的基础营养

> 生长因子再强大，如果细胞缺乏基本的氨基酸和维生素，级联反应也无法启动。以下是当前系统中已识别但尚未实现的基础能力缺口——它们是上述生物学机制落地的前提。

### A. Idea 模型——从信号到意图的缺失层

**现状**：`FeatureRequest` 上有 `idea_id` 字段（models/feature.py:91），数据库有对应列（migration 0003），但 **Idea 模型本身完全缺失**——无 `class Idea`、无 `ideas` 表、无分解逻辑。

**问题**：`goal_decompose` 直接产出 FeatureRequest，跳过了"概念提炼"阶段。相当于生长因子跳过了受体结合直接进入细胞核——信号传导缺少中间层，导致：
- 重复提议相似特性（无法识别"这两个 FR 属于同一个 Idea"）
- 无法做 Idea 级别的可行性评估和优先级排序

**目标**：引入 Idea 生命周期（`proposed → approved → decomposed → completed`），作为 Goal 和 FeatureRequest 之间的中间层。FeatureRequest 通过 `idea_id` 关联回 Idea，支持 Idea 级别的进度追踪和去重。

---

### B. 目标分解的感知盲区

**现状**：
- `goal_decompose.md.j2` 接受 `kpi_status` 参数，但生产环境传入 `recent_snapshots=[]`（loop.py:1104）——KPI 状态形同虚设
- `deployed_features` 完全未接入——分解器看不到已部署的特性列表，导致重复提议

**问题**：目标分解器是"瞎子"——它不知道系统当前处于什么状态、之前做过什么。如同免疫系统的 B 细胞不检查抗体库就生产新抗体——大量重复浪费。

**目标**：
1. 在 `decomposer.py` 中实际采集 KPI 快照并传入分解 prompt
2. 在 `builders.build_goal_decompose()` 中注入 `deployed_features`（近 30 天已部署/已验证特性列表）
3. 为派生特性（`parent_id is not None`）实现自动审批规则，跳过重复评估

---

### C. 数据驱动的验证基础设施

**现状**：`feature_verify.md.j2` 有"结果导向验证"section（含 `metrics_before`/`metrics_after` 字段），解析器也支持提取这些字段。但 **无真实指标采集机制**——验证依赖 LLM 主观判断而非实际度量。

**问题**：这相当于免疫系统通过"感觉还行"来判断病原体是否被消灭，而不是通过抗体滴度检测。无量化标准，验证结论不可靠。

**目标**：建立 `_collect_metrics()` 机制（类似 channels-monitor 的实现），在部署前后自动采集关键指标（测试覆盖率、构建时间、lint 警告数等），用量化阈值（如 `quality_score_delta >= 0.5`）驱动验证判定，将 LLM 判断作为补充而非唯一依据。

---

### D. Workspace 健康检查——修复前的"术前检查"

**现状**：`fix_issue.md.j2` 无任何工作区健康检查。Pipeline 代码中有 `_has_actual_changes()` 硬门禁（feature_pipeline.py:1042），但仅在事后检查。

**问题**：Agent 在脏工作区中工作（未提交变更、活跃的 rebase、worktree 冲突），导致操作失败后需要昂贵的 heal 循环——相当于在感染的伤口上做手术。

**目标**：在 `fix_issue.md.j2` 和 `feature_develop.md.j2` 的执行起点注入工作区健康检查（uncommitted changes、rebase 状态、worktree 锁），失败时提前终止而非事后修复。在 pipeline 层面也增加 pre-flight check，在调用 Agent 前验证 worktree 状态。

---

### E. 环境感知增强（auto_discover）

**现状**：`configurator.py` 的 `auto_discover()` 仅覆盖 5-7 个字段（project.name、description、deploy.url、test_command、build_command），覆盖率 <50%。

**问题**：init 时用户需要手动填充大量配置，增加了生长因子"注入"阶段的摩擦。

**目标**：扩展自动发现能力——从 Dockerfile/.github/workflows 推断 deploy_method，从 CI 配置提取 lint/typecheck 命令，从 README 提取 health_endpoint，将覆盖率提升至 70%+，降低注入阶段的人工介入。

---

按 ROI（投入产出比）排序：

| 优先级 | 方向 | 预期效果 | 工作量 |
|--------|------|----------|--------|
| P0 | 技能胶囊系统 | 修复成功 → 提炼 → 复用，fast-path 自动生长 | 中 |
| P0 | API 令牌桶硬限 + p53 机制 | 防止失控循环和过度增殖 | 小 |
| P0 | 目标分解感知补全 (§B) | KPI 注入 + deployed_features 去重，消除重复提议 | 小 |
| P0 | Workspace 健康检查 (§D) | 消除 70%+ heal 失败，术前检查代替术后修复 | 小 |
| P1 | 知识遗忘/GC（自噬） | 防止知识图谱膨胀降低 Agent 效果 | 小 |
| P1 | L2 Episodic Memory | 显著提升修复命中率 | 中 |
| P1 | 表观遗传层（epigenome.json） | 让系统因环境而异、因经历而特化 | 中 |
| P1 | 数据驱动验证 (§C) | 量化指标替代 LLM 主观判断，验证可信度提升 | 中 |
| P1 | Idea 模型 (§A) | Goal → Idea → FR 三层结构，支撑去重和进度追踪 | 中 |
| P2 | 多模态奖励信号 | 形成正向反馈循环 | 中 |
| P2 | Worktree 物理隔离（血脑屏障） | 补全安全底座 | 小 |
| P2 | 能力外化机制 | 让 Vivify 的知识转化为项目原生能力 | 中 |
| P2 | 环境感知增强 (§E) | auto_discover 覆盖率 50% → 70%+，降低注入摩擦 | 中 |
| P3 | 动态 Agent 工厂 | 长期价值，需较大架构调整 | 大 |
| P3 | 算力弹性调配 | 资源利用率优化 | 中 |
| P3 | 跨项目表观遗传 | 多项目部署时的经验传递 | 大 |

---

## 设计原则

1. **催化而非替代**：Vivify 的每次行为都应增强项目自身的能力，而非创造对 Vivify 的依赖。
2. **融入而非驻留**：成功的标志是 Vivify 的浓度自然递减，而非持续活跃。
3. **分区保护**：绝对保护区通过物理结构而非规则实现不可触达。
4. **代谢平衡**：生长速度不超过消化速度——内置 p53 在过度增殖时主动停机。
5. **可塑性窗口**：早期快速适应、后期稳定抗扰——防止成熟系统被噪音重写。
6. **无损回滚**：任何 Agent 产生的变更都必须可在 60 秒内完全回滚。
7. **能力转移**：Vivify 的知识最终必须外化为项目可独立维护的资产。

---

## 统一框架

| 生物机制 | 在 Vivify 中的角色 | 核心问题 |
|----------|---------------------|----------|
| 自噬 | 激活项目自身的清理能力 + 系统组件的能量竞争淘汰 | 什么该从系统中消失？ |
| 表观遗传 | 让同一套代码对不同项目有不同"表达" + 环境印记 | 什么该保留但改变"音量"？ |
| 分区保护 | 通过物理结构保护宪法级组件不受自噬和表观沉默 | 什么永远不能被触碰？ |
| p53 肿瘤抑制 | 检测过度增殖信号时主动停机 | 什么时候该停止生长？ |
| 生长因子级联 | 修复的同时注入防复发机制，让项目自己变强 | 如何让能力从 Vivify 转移到项目？ |
| 自分泌回路 | 用 Vivify 来 Vivify——自举进化，受哥德尔约束保护 | 系统能否安全地进化自身？ |

---

## Self-Vivification：自举循环

> 如果 Vivify 是生长因子，那把它指向自己的仓库会发生什么？——一个自分泌回路（Autocrine loop）。

### 生物学对应：自分泌回路

生长因子不只作用于普通细胞，也作用于产生生长因子的细胞本身。肝细胞生长因子（HGF）由间充质细胞分泌，但同时也促进间充质细胞自身的增殖：

```
细胞 → 分泌生长因子 → 作用于自身 → 增殖/分化 → 分泌更多生长因子
```

正常情况下，这个正反馈环被精密调控，驱动组织再生。失控时——癌症。

### 自举的四个阶段

**阶段一：自我诊断**

Vivify 的探针扫描自己的代码——发现类型标注不完整、测试覆盖率不足、文档与功能不同步、废弃的实验性代码。这一步没有递归问题，就是正常的代码扫描。

**阶段二：自我修复**

Agent 修复 Vivify 自身的 bug。这里出现第一层递归：**修复代码的执行引擎本身有 bug，它能否正确修复自己？**

类比编译器自举（Compiler Bootstrapping）——GCC 用旧版 GCC 编译自己：

1. 用当前有 bug 的 Vivify(v1) 生成一个修复 PR
2. PR 被合并后产生 Vivify(v2)
3. Vivify(v2) 接管运行，验证修复是否有效
4. 如果 v2 也有问题，继续修复产生 v3...

**阶段三：自我进化**

Vivify 通过 Goals 机制分解自身的 ROADMAP（即本文档）：
- 从 `ROADMAP_LIVING_SYSTEM.md` 中提取目标
- 自动分解为 FeatureRequest："实现技能胶囊系统"、"实现知识 GC"
- Agent 在 worktree 中开发这些功能
- 通过 PR 合并到自身

这是最深层的递归——Vivify 在实现让自己更强大的功能。比如开发"自噬机制"——一旦完成并合并，它获得自噬能力，然后用这个新能力淘汰自己早期的低效代码。

**阶段四：自我约束的不可递归性**

如果 Vivify 修改了 `self_grow_guard.py`——负责约束自身修改的模块——它是否可以解除自己的约束？

这等价于：**p53 基因能否被肿瘤自身突变掉？** 在生物学中，答案是"是"——这正是癌症最危险的变异之一。

因此自举中最关键的设计约束：

> **约束自身修改的模块，必须对自举循环也不可触达。即使是 Vivify 修复自己，宪法级文件也必须走人类 review。**

这不是对 Vivify 能力的不信任，而是逻辑上的必然——一个系统不能验证自己的验证器。这是**哥德尔不完备定理的工程化表达**：

> 一个足够强大的系统可以描述自身的大部分，但不能描述"描述自身"这个行为本身。

Vivify 可以进化自己的探针、修复器、知识图谱、表观层——但不能进化"决定什么可以进化、什么不可以"的那层规则。那层规则是人类保留的最后权柄。

### 自举的实施形态

```yaml
# .vivify.yml (in vivify's own repo)
preset: library
goals:
  path: ROADMAP_LIVING_SYSTEM.md
pr_mode:
  auto_merge: false           # 自举永不自动合并
  draft_default: true         # 所有自举 PR 都是 draft
  labels: ["vivify:self-bootstrap"]
self_growth:
  kernel_modification: pr_with_two_approvals  # 宪法级变更需要双人审批
  plugin_modification: draft_pr               # 即使插件层也走 draft
```

关键规则：
- **所有自举 PR 都是 draft**——人类必须 review
- **宪法级文件需要双人审批**——防止 p53 被突变
- **每轮自举后运行完整测试套件**——用 v(n+1) 的测试验证 v(n+1) 的代码
- **保留 v(n) 的可运行快照**——如果 v(n+1) 出问题可以回滚到上一个已知良好版本

### 自举的三重价值

**1. Dogfooding 的极致形态**

Vivify 是自己最严格的用户。它发现的每一个关于自身的问题（CI 不稳定、文档过时、测试不足），都意味着同类项目也可能有同样问题。修复自身 = 修复框架对该类问题的感知能力。

**2. 加速进化**

传统开源项目的进化速度受限于维护者精力。如果 Vivify 能自己给自己提 PR（在人类设定的 GOALS 和宪法约束内），进化速度大幅提升——人类只需做"方向把控"和"宪法修订"。

**3. 活体文档**

Vivify 自己运行时产生的知识图谱、技能胶囊、表观印记，同时就是关于 Vivify 自身的最佳文档——它不会过时，因为它是运行时实际产生的。

---

## 结语

Vivify 的终极意义不只是"让项目更健康"，而是让软件项目获得一种新的复杂度层次——一个没有 AI 生长因子的项目，在可探索的复杂度空间上有天花板（受限于人类注意力和精力）。注入生长因子后，天花板被抬高——项目可以维护更多边界条件、追踪更深因果链、响应更快速变化。

但这种复杂度的维持不应永远依赖 Vivify 的存在。生长因子的最高境界是：

> **注入 → 激活 → 融入 → 消失。留下的不是自己的痕迹，而是系统焕然一新的生命力。**

而当 Vivify 指向自己时，它进入了一个更高阶的存在——不只是催化别人，也在催化自身的进化。这个自分泌回路被哥德尔约束所限制、被人类审批所守护，在约束中实现指数级自我超越。

不要重写你的项目，让它自己长出来。而 Vivify 是那颗种子里的生长因子——不是园丁，不是土壤，是让种子破壳的那个信号分子。当它作用于自身时，它成为了一颗能让自己不断破壳的种子。
