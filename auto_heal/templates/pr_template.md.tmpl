## auto-heal pull request

**Trigger**: {{ trigger }}
**Round**: {{ round_num }}
**Run id**: `{{ run_id }}`

### What
{{ summary }}

### Why
{{ rationale }}

### Quality checks
```
{{ quality_summary }}
```

### Notes for reviewers
- This PR was opened automatically by [auto-heal](https://github.com/auto-heal/auto-heal).
- The branch lives at `{{ branch }}` and was created from `{{ base_ref }}`.
- Labels: {{ labels | join(", ") }}
- If anything looks wrong, close the PR and `auto-heal` will retry on the next round.
